name: TESTS

on:
  pull_request: null
  push:
    branches:
      - master
      - develop

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: ["8.2", "8.3", "8.4"]

    name: PHP ${{ matrix.php }} tests
    steps:
      # basically git clone
      - uses: actions/checkout@v7

      # use PHP of specific version
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none # disable xdebug, pcov

      # teak (docs generation) requires PHP >=8.3 or an incompatible reflection-docblock version, and isn't needed to run tests
      - run: composer remove --dev timber/teak --no-update

      # if we 2 steps like this, we can better see if composer failed or tests
      - run: composer update --no-progress

      - run: vendor/bin/phpunit
